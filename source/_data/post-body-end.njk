<div class="license">
    <div class="license-title">{{ page.title }}</div>
    <div class="license-link">
        <a href="{{ page.permalink }}" rel="external nofollow noopener" target="_blank">{{ page.permalink }}</a>
    </div>
    <div class="license-meta">
        <div class="license-meta-item">
            <div class="license-meta-title">本文作者</div>
            <div class="license-meta-text">
                {%- if post.original %}
                    {{ post.original }}
                {%- else %}
                    {{ author }}
                {%- endif %}
            </div>
        </div>
        <div class="license-meta-item">
            <div class="license-meta-title">许可协议</div>
            <div class="license-meta-text">
                {%- if post.original %}
                    原作者许可协议
                {%- else %}
                    <a href="/copyright/" rel="external nofollow noopener" target="_blank">著作权声明</a>
                {%- endif %}
            </div>
        </div>
        {%- if post.publication %}
            <div class="license-meta-item">
                <div class="license-meta-title">发表于</div>
                <div class="license-meta-text">
                    {%- if post.publication_url %}
                        <a href="{{ post.publication_url }}" rel="external nofollow noopener" target="_blank">{{ post.publication }}</a>
                    {% else %}
                        {{ post.publication }}
                    {%- endif %}
                </div>
            </div>
        {%- endif %}
    </div>
    <div>转载或引用本文时请遵守著作权许可协议！</div>
</div>
{%- if not post.original %}
    <div class="donations">
        <div class="donation-label">喜欢这篇文章？考虑打赏一下作者吧～</div>
        <a class="afdian-button" href="https://afdian.net/@leirock" rel="external nofollow noopener" target="_blank">
            <i class="fab fa-fw fa-alipay"></i>爱发电<i class="fab fa-fw fa-weixin"></i>
        </a>
    </div>
{%- endif %}